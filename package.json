{
  "name": "job",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tooling only: husky and commitlint. The Python project does not depend on Node.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.0",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7"
  }
}
